package ort.edu.ar.tp3.primerparcial.grupo4.ui.adapters

/*import android.view.LayoutInflater
import android.view.ViewGroup
import androidx.lifecycle.MutableLiveData
import androidx.recyclerview.widget.RecyclerView
import ort.edu.ar.tp3.primerparcial.grupo4.R
import ort.edu.ar.tp3.primerparcial.grupo4.data.entities.Car
import ort.edu.ar.TP3.primerParcial.Grupo4.data.entities.Categoria
import ort.edu.ar.tp3.primerparcial.grupo4.ui.holders.CarViewHolder

class CarListAdapter(
    private val carList: MutableLiveData<List<Car>>
) : RecyclerView.Adapter<CarViewHolder> () {

    val carList2: List<Car>? = carList.value


    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): CarViewHolder {
        val view = LayoutInflater.from(parent.context).inflate(R.layout.recycler_car_info, parent, false)
        return CarViewHolder(view)
    }

    /*override fun getItemCount(): Int {
       return carList2.size
    }

    override fun onBindViewHolder(holder: CarViewHolder, position: Int) {
        val categoria = carList[position]
        holder.setModel(categoria.model)
        holder.setImagen(categoria.make)
        holder.setGasoline(categoria.fuelType)
        holder.setTransmition(categoria.transmission)
        holder.setYear(categoria.year)
        holder.setSeater(categoria.brand)
    }*/


}*/